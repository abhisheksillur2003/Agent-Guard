apiVersion: 1

providers:
  - name: AgentGuard
    orgId: 1
    folder: AgentGuard
    type: file
    disableDeletion: true
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
